---
description: Sherwood implementation plan — mark Done with two-line summary
alwaysApply: true
---

# Implementation Plan — Fractional Asset Loot Protocol

## Phase 0 — Knowledge base + product pivot scaffold
- [x] cursor_project_rules (README, tech-stack, this plan)
- [x] DEVELOPMENT.md changelog
- [x] Decide: greenfield Pot stack vs evolve RaffleManager
- [x] PRD locked as source of truth (ownership-for-all, not lose-everything)

## Phase 1 — Tracer bullet (contracts)
- [x] PotFactory + Pot: create, deposit, close on target/deadline
- [x] Card NFT mint on deposit (unrevealed)
- [x] RevealEngine: VRF → ownership weights sum to 1e18 (100%) + rarity tiers
- [x] AssetManager stub: swap USDG → target token into pot vault
- [x] Forge tests: funding, close, reveal conservation (Σ ownership = 100%)

## Phase 2 — Tracer bullet (web)
- [x] Pot discovery list (concurrent pots, progress bars)
- [x] Deposit flow (USDG approve + join)
- [x] Inventory: mystery → revealed card with ownership %
- [x] Replace raffle landing copy with Fractional Asset Loot positioning
- [x] Claim asset share + list-to-marketplace from inventory

## Phase 3 — Marketplace + gamification (later)
- [x] Card trading / royalties (`CardMarketplace`, 2.5% → Treasury)
- [x] Achievements + leaderboards (`/leaderboard`)
- [x] Community pot creation UI (`/create`)

## Phase 4 — Compliance / allocation IP
- [x] Transparent expected-value docs for allocation algorithm
- [x] Floor ownership guarantee (every card > 0) — enforced in RevealEngine
- [x] Jurisdiction review notes (non-code)

## Phase 5 — Full business flow + fees + audit battery
- [x] Fee paths: creation / entry / protocol → Treasury
- [x] Claim/redeem ownership share of purchased assets
- [x] Pause + double-pull/double-claim guards + operators
- [x] Local anvil deploy + end-to-end simulate (`scripts/devnet-sim.sh`)
- [x] Fuzz + invariant + attack tests; audit checklist

## Phase 6 — Direction v2: OpenSea, exits, derived pricing (2026-07-16)
- [x] 6.1 Fixed-price listings + derived-value display on Trade page (exists: CardMarketplace escrow-by-approval; add derived value + state badges in UI)
- [ ] 6.2 Early exit: pre-close (live, 5% flat) + post-reveal (needs NAV; deferred until oracle path) — post-close/pre-reveal window HELD per rollout
- [x] 6.3 EIP-2981 (2.5%) + contractURI + dynamic metadata v2 → OpenSea collection submission (contracts+API ready; submit after RH redeploy)
- [x] 6.4 On-chain `derivedShare(tokenId)` view (pre-reveal deposit share / post-reveal weight); NAV multiplication stays off-chain
- [x] 6.5 Marketplace shell-card guard: `buy()`/`list()` reject claimed cards; document listing-lock rule
- [x] 6.6 Docs: new early-exit / secondary-market / pricing / opensea pages; amend allocation + fees
- [ ] 6.7 Security: reentrancy audit pass on exit/claim/list/buy; VRF migration path documented; external audit before scaling TVL

## Done Log

### Phase 0 (partial) — Done (2026-07-13)
Created cursor_project_rules from Fractional Asset Loot PRD.
Documented legacy raffle/pack as non-core; pending architecture fork decision.

### Phase 0 decision + Phase 1 — Done (2026-07-13)
Chose greenfield Pot stack; reuse USDG/VRF/Treasury/swap patterns from raffle prototype.
Added PotFactory, Pot, PotCard, RevealEngine, AssetManager + PotProtocol forge tests (4/4 pass).
Allocation: deposit-weighted multipliers [0.5x–2.0x], normalize to 1e18, floor > 0.

### Phase 2 tracer — Done (2026-07-13)
Web pot discovery + deposit hook, cards inventory, landing copy pivoted to Fractional Asset Loot.
ABIs exported; PotDeploy.s.sol + env example for factory/card addresses.

### Phase 5 — Done (2026-07-13)
Wired Treasury fee collection, claim flow, pause/operators, localdevnet sim (`SIMULATION_OK`).
Audit tests 16/16 (incl. fuzz 256 + invariant 128k calls); checklist in audit-checklist.mdc.

### Phases 2–4 complete — Done (2026-07-13)
Claim/list UX; CardMarketplace + royalty tests; create pot / market / leaderboard pages.
Allocation EV + jurisdiction notes in cursor_project_rules; `/docs/allocation` published.

### Sherhood RH prep — Done (2026-07-13)
EntryRouter ETH/WETH→USDG + fee; StockTokenRegistry allowlist; Sherhood UI; RhDeploy.s.sol.
Pushed to https://github.com/David-glitc/sherhood

### RH gap close (VRF + Router02) — Done (2026-07-13)
Chainlink VRF not on RH (4663): RhDeploy auto-deploys PrevRandaoCoordinator when VRF_COORDINATOR unset.
Switched EntryRouter/AssetManager to SwapRouter02 ABI; RH router `0xcaf681a66d020601342297493863e78c959e5cb2`.

### Responsive product surfaces — Done (2026-07-17)
Added shared responsive containers, product surfaces, focus and reduced-motion rules; rebuilt app, create, inventory, Trade, leaderboard, profile, docs, and Telegram layouts.
Completed explicit loading, empty, error, connected/disconnected, pending, and success states; lint, TypeScript, and production build pass.

### Editorial landing and user docs — Done (2026-07-17)
Rebuilt the landing page around an asymmetric editorial hero, animated card object, flat information flow, responsive ownership preview, and clearer conversion path.
Rewrote every MDX page as a user guide; removed formulas, contract internals, deployment language, and unexplained implementation terms. Lint, TypeScript, and production build pass.

### Domain sherhood.xyz + Vercel — Done (2026-07-13)
Canonical domain sherhood.xyz; Vercel project web production deploy + RH env vars.
CF A records script `scripts/cf-dns-sherhood.mjs` — pending refreshed API token (VPS IP allowlist).

### Docs MDX site — Done (2026-07-15)
Shipped `/docs` with getting-started + protocol + allocation + fees via next-mdx-remote RSC (deterministic SSG).
Aligned docs with multi-stock-at-close + $SHRH luck; cleaned docs/create layouts.

### Basket detail + early exit — Done (2026-07-15)
Added Pot earlyExit (5% fee → treasury, burn card) + `/basket/[address]` UI with withdraw.
Existing live pots need new Pot bytecode (new factory creates) for on-chain early exit.

### Phase-two landing chrome — Done (2026-07-16)
Custom glass wallet chip (RainbowKit Custom) + cinematic full-viewport hero with parallax stock logos.
Landing nav fades under hero; glass-panel / glass-ray system for premium surfaces.

### Direction v2 tranche 1 — Done (2026-07-16)
PotCard: EIP-2981 2.5% + contractURI. Pot: derivedShare view. Market: reject claimed shells.
Metadata API v2 + /api/collection; docs early-exit/secondary/pricing/opensea; Trade/Inventory state badges + fair share.
OpenSea collection submit + RH redeploy still required for on-chain royalty/URI to go live.

### Fresh stack + dual luck + XP — Done (2026-07-16)
Branch `contracts/sherhood-v2-fresh`. SherhoodDeploy.s.sol clean slate (no $SHRH mint). Dual luck check (deposit lock + reveal).
Docs stripped of internals; XP API + leaderboard + /docs/xp. Threshold target ~0.055 ETH of $SHRH set later via setLuckToken.
Await deployer funding (~0.004 ETH) then `scripts/sherhood-deploy.sh` with Blockscout verify.
