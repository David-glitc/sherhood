---
description: Sherwood implementation plan — mark Done with two-line summary
alwaysApply: true
---

# Implementation Plan — Fractional Asset Loot Protocol

## Phase 0 — Knowledge base + product pivot scaffold
- [x] cursor_project_rules (README, tech-stack, this plan)
- [x] DEVELOPMENT.md changelog
- [x] Decide: greenfield Pot stack vs evolve RaffleManager
- [x] PRD locked as source of truth (ownership-for-all, not lose-everything)

## Phase 1 — Tracer bullet (contracts)
- [x] PotFactory + Pot: create, deposit, close on target/deadline
- [x] Card NFT mint on deposit (unrevealed)
- [x] RevealEngine: VRF → ownership weights sum to 1e18 (100%) + rarity tiers
- [x] AssetManager stub: swap USDG → target token into pot vault
- [x] Forge tests: funding, close, reveal conservation (Σ ownership = 100%)

## Phase 2 — Tracer bullet (web)
- [x] Pot discovery list (concurrent pots, progress bars)
- [x] Deposit flow (USDG approve + join)
- [x] Inventory: mystery → revealed card with ownership %
- [x] Replace raffle landing copy with Fractional Asset Loot positioning
- [x] Claim asset share + list-to-marketplace from inventory

## Phase 3 — Marketplace + gamification (later)
- [x] Card trading / royalties (`CardMarketplace`, 2.5% → Treasury)
- [x] Achievements + leaderboards (`/leaderboard`)
- [x] Community pot creation UI (`/create`)

## Phase 4 — Compliance / allocation IP
- [x] Transparent expected-value docs for allocation algorithm
- [x] Floor ownership guarantee (every card > 0) — enforced in RevealEngine
- [x] Jurisdiction review notes (non-code)

## Phase 5 — Full business flow + fees + audit battery
- [x] Fee paths: creation / entry / protocol → Treasury
- [x] Claim/redeem ownership share of purchased assets
- [x] Pause + double-pull/double-claim guards + operators
- [x] Local anvil deploy + end-to-end simulate (`scripts/devnet-sim.sh`)
- [x] Fuzz + invariant + attack tests; audit checklist

## Phase 6 — Direction v2: OpenSea, exits, derived pricing (2026-07-16)
- [x] 6.1 Fixed-price listings + derived-value display on Trade page (exists: CardMarketplace escrow-by-approval; add derived value + state badges in UI)
- [ ] 6.2 Early exit: pre-close (live, 5% flat) + post-reveal (needs NAV; deferred until oracle path) — post-close/pre-reveal window HELD per rollout
- [x] 6.3 EIP-2981 (2.5%) + contractURI + dynamic metadata v2 → OpenSea collection submission (contracts+API ready; submit after RH redeploy)
- [x] 6.4 On-chain `derivedShare(tokenId)` view (pre-reveal deposit share / post-reveal weight); NAV multiplication stays off-chain
- [x] 6.5 Marketplace shell-card guard: `buy()`/`list()` reject claimed cards; document listing-lock rule
- [x] 6.6 Docs: new early-exit / secondary-market / pricing / opensea pages; amend allocation + fees
- [ ] 6.7 Security: reentrancy audit pass on exit/claim/list/buy; VRF migration path documented; external audit before scaling TVL

## Phase 7 — Product surfaces + moat (2026-07-18)
- [x] 7.1 Relay bridge page (`/bridge`) — all Relay chains, default destination Robinhood Chain
- [x] 7.2 Roadmap page (`/roadmap`)
- [x] 7.3 Create fee UX — subtle fee; USDG or ETH; balance detection
- [x] 7.4 $SHRH hold-to-waive + buy widget (Relay swap when live)
- [x] 7.5 Mobile nav — Bridge + Deck + Roadmap
- [x] 7.6 Factory `createFor` + `/api/create-sponsored` (deployer signer; needs factory redeploy with createFor)
- [x] 7.10 On-site product deck (`/deck`, 9 slides)
- [x] 7.11 Full profile dashboard (deposits, mark/PnL, cards, XP, listings)
- [ ] 7.7 Generative Gacha Cards / DMT card layers from asset + rarity + pool share
- [ ] 7.7b Single-card swaps — stock bundle → instant tradable card
- [ ] 7.8 Index baskets + instant NAV redeem
- [ ] 7.9 Elite coins listing ($500 + manual DD)

## Done Log

### Phase 0 (partial) — Done (2026-07-13)
Created cursor_project_rules from Fractional Asset Loot PRD.
Documented legacy raffle/pack as non-core; pending architecture fork decision.

### Phase 0 decision + Phase 1 — Done (2026-07-13)
Chose greenfield Pot stack; reuse USDG/VRF/Treasury/swap patterns from raffle prototype.
Added PotFactory, Pot, PotCard, RevealEngine, AssetManager + PotProtocol forge tests (4/4 pass).
Allocation: deposit-weighted multipliers [0.5x–2.0x], normalize to 1e18, floor > 0.

### Phase 2 tracer — Done (2026-07-13)
Web pot discovery + deposit hook, cards inventory, landing copy pivoted to Fractional Asset Loot.
ABIs exported; PotDeploy.s.sol + env example for factory/card addresses.

### Phase 5 — Done (2026-07-13)
Wired Treasury fee collection, claim flow, pause/operators, localdevnet sim (`SIMULATION_OK`).
Audit tests 16/16 (incl. fuzz 256 + invariant 128k calls); checklist in audit-checklist.mdc.

### Phases 2–4 complete — Done (2026-07-13)
Claim/list UX; CardMarketplace + royalty tests; create pot / market / leaderboard pages.
Allocation EV + jurisdiction notes in cursor_project_rules; `/docs/allocation` published.

### Sherhood RH prep — Done (2026-07-13)
EntryRouter ETH/WETH→USDG + fee; StockTokenRegistry allowlist; Sherhood UI; RhDeploy.s.sol.
Pushed to https://github.com/David-glitc/sherhood

### RH gap close (VRF + Router02) — Done (2026-07-13)
Chainlink VRF not on RH (4663): RhDeploy auto-deploys PrevRandaoCoordinator when VRF_COORDINATOR unset.
Switched EntryRouter/AssetManager to SwapRouter02 ABI; RH router `0xcaf681a66d020601342297493863e78c959e5cb2`.

### Responsive product surfaces — Done (2026-07-17)
Added shared responsive containers, product surfaces, focus and reduced-motion rules; rebuilt app, create, inventory, Trade, leaderboard, profile, docs, and Telegram layouts.
Completed explicit loading, empty, error, connected/disconnected, pending, and success states; lint, TypeScript, and production build pass.

### Editorial landing and user docs — Done (2026-07-17)
Rebuilt the landing page around an asymmetric editorial hero, animated card object, flat information flow, responsive ownership preview, and clearer conversion path.
Rewrote every MDX page as a user guide; removed formulas, contract internals, deployment language, and unexplained implementation terms. Lint, TypeScript, and production build pass.

### Domain sherhood.xyz + Vercel — Done (2026-07-13)
Canonical domain sherhood.xyz; Vercel project web production deploy + RH env vars.
CF A records script `scripts/cf-dns-sherhood.mjs` — pending refreshed API token (VPS IP allowlist).

### Docs MDX site — Done (2026-07-15)
Shipped `/docs` with getting-started + protocol + allocation + fees via next-mdx-remote RSC (deterministic SSG).
Aligned docs with multi-stock-at-close + $SHRH luck; cleaned docs/create layouts.

### Basket detail + early exit — Done (2026-07-15)
Added Pot earlyExit (5% fee → treasury, burn card) + `/basket/[address]` UI with withdraw.
Existing live pots need new Pot bytecode (new factory creates) for on-chain early exit.

### Phase-two landing chrome — Done (2026-07-16)
Custom glass wallet chip (RainbowKit Custom) + cinematic full-viewport hero with parallax stock logos.
Landing nav fades under hero; glass-panel / glass-ray system for premium surfaces.

### Direction v2 tranche 1 — Done (2026-07-16)
PotCard: EIP-2981 2.5% + contractURI. Pot: derivedShare view. Market: reject claimed shells.
Metadata API v2 + /api/collection; docs early-exit/secondary/pricing/opensea; Trade/Inventory state badges + fair share.
OpenSea collection submit + RH redeploy still required for on-chain royalty/URI to go live.

### Fresh stack + dual luck + XP — Done (2026-07-16)
Branch `contracts/sherhood-v2-fresh`. SherhoodDeploy.s.sol clean slate (no $SHRH mint). Dual luck check (deposit lock + reveal).
Docs stripped of internals; XP API + leaderboard + /docs/xp. Threshold target ~0.055 ETH of $SHRH set later via setLuckToken.
Await deployer funding (~0.004 ETH) then `scripts/sherhood-deploy.sh` with Blockscout verify.

### Phase 7 shipping tranche — Done (2026-07-18)
Shipped `/bridge` (Relay SwapWidget: all chains → Robinhood), `/roadmap`, `/deck`, subtle create-fee UX with USDG|ETH pay + balances, $SHRH buy-widget shell + waiver gate, mobile nav with Bridge/Roadmap/Deck, profile mark/PnL.
Sponsored create uses existing owner `createPot` (no factory upgrade). Deferred: generative cards, index redeem, Elite listings.

### Phase 7 — Bridge + roadmap + create fee UX — Done (2026-07-18)
Shipped `/bridge` with Relay SwapWidget (ETH / Base / Solana presets), RelayKitProvider + mainnet/base in wagmi.
Create page: subtle fee (no Pay $5 CTA), USDG/ETH fee pay via wrap+swap, balance detect, $SHRH buy widget + $500 waiver gate.
`/roadmap` lists Gacha Cards, index redeem, Elite coins, deck, profile dashboard; mobile nav reordered with Bridge.

### USDG 6-dec create harden — Done (2026-07-19)
Web + scripts reject 18-dec pot params; Crimson Talon \$5 pot live; legacy ETH fund disabled in UI.
Create/fee/balance paths use native 6-decimal USDG units end-to-end.

### Multi-hop stock purchase + Sherds — Done (2026-07-19)
MultiHopSwapAdapter (USDG→WETH→stock) live; 2-pick liquid registry (TSLA/USO); Crimson Talon purchased+revealed.
Product units renamed Sherds; OpenSea deep-link for PotCard on Robinhood Chain.

### SEO + share surfaces — Done (2026-07-19)
Dynamic OG images from brand banner for home/basket/Sherd; generateMetadata + Product/Breadcrumb JSON-LD; open pots in sitemap.
Share buttons on basket detail + inventory; public `/sherd/[id]` share landing; hero uses full-bleed banner.

### Profile identity + onchain rarity display — Done (2026-07-19)
OpenSea/metadata reads on-chain rarity at reveal; UI falls back to ownership bands only for legacy cards.
Users set name + one of 10 SVG hoods on Profile; chips render on leaderboard, basket deposits, and Trade.

### People discovery + allow-receive send — Done (2026-07-19)
Public `/people` + `/u/[address]`; Allow receive gates address display and send targets.
Leaderboard/People in header+footer; OpenSea collection API updated for Sherds + brand banner.

### Unique profile names + share — Done (2026-07-19)
Profiles keyed by unique name slug (`/u/name`); share buttons on profile/people; UI copy moved to tooltips.

### Cards UX + stock NAV + deck + deploy — Done (2026-07-19)
Inventory animations + claimable amount/price/value; TradingView minis; profile PnL fixed for USDG decimals.
Deck is full-bleed (back + slides + banner/logos). Production deploy on Vercel project web → sherhood.xyz.

### Leaderboard + profile balances + wallet NFTs — Done (2026-07-19)
XP leaderboard render + People/Send; profile view mode + wallet/Sherd values; Add Sherds via wallet_watchAsset; hero prism restored; TradingView theme aligned.
Send typeahead by unique name with pfp + truncated address.

### Sherd pools + Orynth #1 — Done (2026-07-19)
Renamed UI baskets→Sherd pools; nav icons; created Orynth #1 pool ($100 / $2 min / 5d) at 0x80D6…5910.

### MongoDB profiles + web perf — Done (2026-07-19)
Profiles persist in Atlas; Relay/landing lazy-load; production redeployed.

### Hero + Mongo account data + legal — Done (2026-07-19)
Restored reference hero; XP/streaks/profiles in Atlas; Delete my account; deep Terms/Privacy.

### Telegram activity bot — Done (2026-07-19)
Bootstrapped TG broadcast cron for pool/fund/trade/transfer events; set TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID to go live.

### TG webhook + poller (no cron UI) — Done (2026-07-19)
Webhook + `/whereami` for chat id; `tg:poll` long worker for chain broadcasts (Chessonchain-style).
Removed public cron-secret UI; cleaned navbar socials as external links (X/TG/OpenSea).

### Dynamic pools + onboarding — Done (2026-07-19)
Pool discovery motion/hierarchy upgrade; profile hero; first-visit walkthrough + finish-profile popup for connected wallets without a name.

### Public profile cards + buy/offer — Done (2026-07-19)
`/u/[slug]` inventory sections; send gated on login; marketplace buy + off-chain offers.

### Card CLS + Sherd detail — Done (2026-07-19)
Compact Sherd tiles (no CLS); dark multi-symbol TV tabs; /sherd owner + asset shares; clean ownership %.

### $SHRH docs + OG Twitter contrast — Done (2026-07-19)
Added /docs/shrh (hold-only); scrubbed hold amounts from UI copy; high-contrast OgFrame + twitter-image for basket/sherd; Twitter @sherhood_xyz cards.

### Twitter OG build fix + hold-only polish — Done (2026-07-19)
Fixed Next twitter-image (shared renderers, no config re-export); solid lime-on-black OG plate; docs/roadmap say hold only (no amounts).

### V2 docs + distribution features — Done (2026-07-19)
Published /docs/v2 with remaining product track and expanded distribution (referrals, seasons, creator share, LP, quests).
Knowledge mirror: cursor_project_rules/v2.md.

### Form V2 docs — Done (2026-07-19)
Reformed /docs/v2 into layered tables (product, distribution A–H, ship order); knowledge mirror updated.

### Profile PnL + balances + charts — Done (2026-07-20)
Profile: dynamic balances, colored live Mark PnL, allocation/mark charts, denser layout, shareable /api/og/pnl cards.

### WebSpeed landing optimisation — Done (2026-07-20)
Deferred wallet on marketing, smaller LCP card image, SSR hero, CLS/link/cache fixes for Performance.

### WebSpeed CWV pass — Done (2026-07-20)
Landing LCP image + deferred wallet/orbit JS; header without framer; font-display optional for CLS.

### Lifetime protocol revenue on stats — Done (2026-07-20)
Exposed TreasuryDirect feesForwardedUSDG as Lifetime fees on /app + /api/stats + TG; fees docs list destinations.

### Profile + OpenSea Sherds showcase — Done (2026-07-20)
Tight profile/u layouts; inventory OpenSea-style showcase; all Sherds link to OpenSea; collection URL /collection/sherds.

### Inventory cards + profile links + footer — Done (2026-07-20)
Fitted inventory Sherd cards linking to `/sherd/[id]`; usernames link to `/u/[slug]` via UserChip.
Rebuilt site footer with brand, nav columns, and socials (risk banner removed from hero line).

### Card 3D + profile hero + footer — Done (2026-07-20)
3D mouse-tilt Sherd cards; card-first grids without fractional boxes; one profile edit hero.
Footer compacted to logo/nav/socials with lime accent edge.

### Card motion + sfx + Buy SHRH + account pack — Done (2026-07-20)
Idle float/shimmer cards; mint/send sounds with mute; Buy $SHRH coming-soon dialog.
Profile/Collection packaged via AccountSubnav; public profile card stack + mark/day delta.

### $SHERD token live — Done (2026-07-20)
Wired CA 0xe429…aCc1; ticker SHERD; SHRH_LAUNCHED on; Buy $SHERD dialog goes to Relay swap.

### Swap fix + lean UX + fund $SHERD — Done (2026-07-20)
Replaced broken Relay buy with Flap/Uniswap; stripped excess fund/app copy.
Added $SHERD pay tab (mark→ETH settle + Uniswap assist) on pool cards and basket.

### In-app $SHERD swap — Done (2026-07-21)
Updated Buy CTA to open an in-app `/buy-shrd` swap screen instead of a dialog.
Ensured Relay provider boots on the new route for the SwapWidget.

### Pool rank + XP soundness + OpenSea SEO — Done (2026-07-21)
Ranked pools by activity; fixed streak expiry and XP indexing; profiles show rank + created pools.
OpenSea collection section on landing (not hero); robots/sitemap crawl policy tightened.

### Profile UX mobile pass — Done (2026-07-22)
Sign out, portfolio tokens, Mark PnL layout, Share PnL modal, ETH ticker, name→profile, dwell mint modal.
Documented Phase 2 Onboarding pool (env deep-link); no on-chain create this ship.

### XP persistence + Mark PnL text — Done (2026-07-23)
XP/streak now event-backed (`xp_events` merge + stale/board fallback); no zero-wipe on RPC fail.
Mark PnL amounts wrap instead of truncating; reveal XP uses owner-at-block.

### Pool lifecycle ops + details — Done (2026-07-24)
Owner/permissionless seal + ops auto purchase/reveal; pool holdings mark/PnL + pulse history.
Footer clearance; Mint Sherd copy; Sherd→pool nav; sealed 0.5×–2× share band wording.

### Footer + live vault + create fee — Done (2026-07-24)
In-flow footer on all pages; vault holdings use live token balances after claims.
Create fee USDG/ETH toggle fixed; pool reads poll on-chain every ~12s.

## Phase 8 — Live discovery + trade surfaces (2026-07-25)
- [x] 8.1 Show verified revealed pools across current and earlier factories
- [x] 8.2 Exclude protocol wallets and empty scores from the public leaderboard
- [x] 8.3 New-pool notifications, stock-logo pool visuals, and live vault charts
- [x] 8.4 Signed off-chain offers for listed or unlisted Sherds
- [x] 8.5 Restore create registry UI (orbit + StockRegistryGrid)

### Phase 8 — Live discovery + trade surfaces — Done (2026-07-25)
Merged verified legacy revealed pools into live discovery; xp-store excludes ops wallets before limit/rank.
Create aside restored to orbit + StockLogoStack + StockRegistryGrid; offers require buyer signature, chain/card binding, owner + claimed checks.

### Phase 8.6 — Orbit logos + Stock Gacha + random names — Done (2026-07-25)
Pool orbit uses HTML StockLogo nodes (SVG image href was unreliable). Leaderboard merges XP + People directory, ranks 0-XP by join time.
Created Stock Gacha pool on-chain; FEATURED merge shows all revealed pots; create page has randomBasketName().

### Phase 8.7 — Defer factory upgrade; V2 disclosure; UI → master — Done (2026-07-25)
Factory/pot hardenings stay on `audit/harden-findings-h1-l8` (no RH redeploy). Site discloses V2 coming soon.
UI/web tree promoted to `master` for production redeploy + perf.

### Phase 8.8 — Orbit logos on dashed ring + pool UX — Done (2026-07-25)
Logos sit on dashed-ring radius (cqmin) and spin with the SVG path; pool hero/mint/raised hierarchy cleaned up.

## Phase 9 — Instant Mint + UX polish (2026-08-01)
- [x] 9.1 Harden advance-pool reveal (allocateWithSeed + fulfill retries)
- [x] 9.2 Discovery view-only cards (View pool / Open vault)
- [x] 9.3 Instant Mint API + client flow ($1.50–$2, deployer create)
- [x] 9.4 Create wizard (Instant vs Community)
- [x] 9.5 $SHERD-first mint quotes + buy-shrd any-token path
- [x] 9.6 Elite landing USP section + Instant Mint copy
- [x] 9.7 Stock Gacha purchase+reveal

### Phase 9 — Instant Mint + UX polish — Done (2026-08-01)
Shipped Instant Mint path, create wizard, discovery view-only, SHERD-first fund panel, landing USPs, allocateWithSeed advance.
Stock Gacha advanced to Revealed after deployer gas top-up (buy + reveal + fee sweep).

## Phase 10 — Polish + Instant Mint upgrade (2026-08-01)
- [x] 10.1 Instant Mint multi-pay (USDG / ETH / $SHERD)
- [x] 10.2 Post-fee book on vault mark PnL
- [x] 10.3 Soft (non-modal) walkthrough + Instant Mint tip
- [x] 10.4 Hero + roadmap Instant Mint live CTAs
- [x] 10.5 Empty vault cancel messaging (on-chain still needs deadline)

### Phase 10 — Polish + Instant Mint upgrade — Done (2026-08-01)
Instant Mint accepts USDG/ETH/$SHERD; vault mark uses net book; walkthrough is a bottom coach strip; landing/roadmap promote Instant Mint.
Empty cancel before deadline remains V2 (Pot.cancel requires expired deadline).

### Phase 10.6 — Create Instant vs pool as tabs — Done (2026-08-01)
Single `/create` page with Instant Sherd | Create pool tabs; old subroutes redirect.
